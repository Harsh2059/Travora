# Copy this file to .env.local and set the URL for your environment.
# .env.local is gitignored — never commit it.

# Option A: Use the live Render backend (no local backend needed)
VITE_API_BASE_URL=https://travora-dqgn.onrender.com/api

# Option B: Run backend locally on port 8000
# VITE_API_BASE_URL=http://localhost:8000/api
